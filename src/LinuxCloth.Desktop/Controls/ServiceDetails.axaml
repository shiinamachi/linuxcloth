<UserControl
    x:Class="LinuxCloth.Desktop.Controls.ServiceDetails"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid RowDefinitions="*,Auto">
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
      <StackPanel Spacing="10">
        <Grid ColumnDefinitions="96,*" ColumnSpacing="8">
          <Border Classes="logo-frame" Width="88" Height="88" HorizontalAlignment="Left" VerticalAlignment="Top">
            <Image Source="{Binding SelectedService.Image}" Stretch="Uniform" />
          </Border>
          <StackPanel Grid.Column="1" Spacing="6">
            <TextBlock
                Text="{Binding SelectedService.DisplayName}"
                Classes="page-title"
                TextWrapping="Wrap"
                TextTrimming="CharacterEllipsis" />
            <StackPanel Orientation="Horizontal" Spacing="6">
              <Border Classes="badge-success" IsVisible="{Binding SelectedService.IsVerified}">
                <TextBlock Text="{Binding SelectedService.CompatibilityLabel}" Classes="meta" Foreground="{DynamicResource SuccessBrush}" />
              </Border>
              <Border Classes="badge-warning" IsVisible="{Binding SelectedService.IsPartial}">
                <TextBlock Text="{Binding SelectedService.CompatibilityLabel}" Classes="meta" Foreground="{DynamicResource WarningBrush}" />
              </Border>
              <Border Classes="badge-danger" IsVisible="{Binding SelectedService.IsBlocked}">
                <TextBlock Text="{Binding SelectedService.CompatibilityLabel}" Classes="meta" Foreground="{DynamicResource DangerBrush}" />
              </Border>
              <Border Classes="badge-neutral" IsVisible="{Binding SelectedService.IsUnknown}">
                <TextBlock Text="{Binding SelectedService.CompatibilityLabel}" Classes="meta" />
              </Border>
              <TextBlock Text="{Binding SelectedService.Category}" Classes="caption" VerticalAlignment="Center" />
            </StackPanel>
            <TextBlock Text="{Binding SelectedService.Url}" Classes="caption" TextWrapping="Wrap" />
          </StackPanel>
        </Grid>

        <TextBlock
            Text="{Binding SelectedService.CompatibilityDescription}"
            TextWrapping="Wrap"
            Classes="caption" />

        <Border Classes="notice-warning">
          <TextBlock
              Text="닫으면 이번 실행의 파일과 변경사항이 삭제됩니다."
              TextWrapping="Wrap"
              Classes="caption warning"
              FontWeight="Bold" />
        </Border>

        <StackPanel Spacing="4">
          <TextBlock Text="Windows 환경" FontWeight="Bold" Classes="caption" />
          <ComboBox
              ItemsSource="{Binding Images}"
              SelectedItem="{Binding SelectedImage}"
              HorizontalAlignment="Stretch"
              AutomationProperties.AutomationId="Catalog.Details.WindowsEnvironment"
              AutomationProperties.Name="서비스를 열 Windows 환경">
            <ComboBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Label}" TextTrimming="CharacterEllipsis" />
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </ComboBox>
          <Button
              IsVisible="{Binding !HasImages}"
              Classes="secondary"
              Content="Windows 환경 준비"
              Click="OnOpenSetup"
              AutomationProperties.AutomationId="Catalog.Details.PrepareWindowsEnvironment"
              HorizontalAlignment="Left" />
        </StackPanel>

        <Expander
            Classes="technical"
            Header="기술 세부정보"
            AutomationProperties.AutomationId="Catalog.Details.TechnicalDetails">
          <StackPanel Spacing="8" Margin="0,8,0,0">
            <StackPanel Spacing="2">
              <TextBlock Text="영문 이름" Classes="meta" />
              <TextBlock Text="{Binding SelectedService.EnglishDisplayName}" TextWrapping="Wrap" Classes="caption" />
            </StackPanel>
            <StackPanel Spacing="2">
              <TextBlock Text="실행 구성" Classes="meta" />
              <TextBlock Text="{Binding SelectedService.PackageSummary}" TextWrapping="Wrap" Classes="caption" />
            </StackPanel>
            <TextBlock
                IsVisible="{Binding SelectedService.HasCustomBootstrap}"
                Text="추가 준비 단계를 사용합니다."
                TextWrapping="Wrap"
                Classes="caption" />
          </StackPanel>
        </Expander>
      </StackPanel>
    </ScrollViewer>

    <Button
        Grid.Row="1"
        Classes="primary"
        Margin="0,12,0,0"
        Content="실행하기"
        Command="{Binding LaunchCommand}"
        IsEnabled="{Binding CanLaunch}"
        HorizontalAlignment="Stretch"
        HorizontalContentAlignment="Center"
        AutomationProperties.AutomationId="Catalog.Launch"
        AutomationProperties.Name="선택한 서비스 열기" />
  </Grid>
</UserControl>
